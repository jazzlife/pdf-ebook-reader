<Window x:Class="BookReader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"

        Title="Book Reader 4"
        WindowStartupLocation="Manual"
        WindowState="{Binding Path=Settings.WindowState, Mode=TwoWay}"
        Left="{Binding Path=Settings.Left, Mode=TwoWay}"
        Top="{Binding Path=Settings.Top, Mode=TwoWay}"
        Width="{Binding Path=Settings.Width, Mode=TwoWay}"
        Height="{Binding Path=Settings.Height, Mode=TwoWay}" 
        
        mc:Ignorable="d" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        d:DesignHeight="218" d:DesignWidth="370" xmlns:my="clr-namespace:BookReaderWPF.View">
    <Grid>
        <my:LibraryView x:Name="libraryView1" />
    </Grid>
</Window>
